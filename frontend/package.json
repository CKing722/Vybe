{
  "name": "vybe-live",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy:staging": "npm run build && wrangler pages deploy dist --project-name=vybe-platform-staging"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0"
  }
}
