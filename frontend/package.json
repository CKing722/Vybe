{
  "name": "vybe-live",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy:cloudflare": "npm run build && npx --yes wrangler@latest pages deploy dist --project-name=vybe-platform-staging --commit-dirty=true",
    "deploy:vercel": "npx --yes vercel@latest --prod --yes",
    "deploy:staging": "npm run deploy:cloudflare && npm run deploy:vercel"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.3"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0"
  }
}
